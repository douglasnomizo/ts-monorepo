{
  "name": "@dhln/api",
  "version": "1.0.0",
  "description": "Connects to DB and exposes an API",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "lint": "eslint src/**/*.ts",
    "test": "jest",
    "migrate": "NODE_PATH=build node dist/migrate.js",
    "start": "NODE_PATH=build node dist/index.js"
  },
  "dependencies": {
    "@dhln/common": "1.0.0",
    "amqplib": "^0.6.0",
    "koa": "^2.13.0",
    "pg": "^8.5.1",
    "typescript": "^4.1.2"
  }
}
